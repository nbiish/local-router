# Suggestions — 2026-09-23

### [2026-09-23T19:05Z] nanoboozhoo/local-router — ui/catalog-state-preserve (merged 5e4d444) + GH013 leak remediation
Providers catalog now edits the live DOM for every selection/search action and snapshot-restores each provider's 360px list scroll (keyed by provider) plus every search box across data rebuilds; "＋ Fallback" stages in place instead of navigating; silent auto-save keeps the canonical status line. Verified on a config-copy instance (scroll 217/155 preserved through toggles and Save Curation rebuilds, filter narrows 448->31 and survives toggling, zero page errors) and shipped via rebuilt router (verify PASSED, catalog 2391). Separately, the first push was rejected by GitHub push protection: a 2026-09-19 PQC-key diagnostic (report.20260919*.json) had been committed with a plaintext OpenRouter API key — purged whole-file from the unpushed history in an isolated clone and re-pushed (remote main = 9ef38cc). Next step: rotate the exposed OpenRouter API key at openrouter.ai and re-pack the replacement into the PQC bundle (provider keys are still missing from all stores).
What would this adversarial / security master suggest? Treat the OpenRouter key as burned (GitHub's scanner and any process reading the report saw it) — rotate first, purge history second, and add secret-scanning (gitleaks) as a pre-commit gate so diagnostic dumps can never land again.
What would this privacy / data-protection regulatory master suggest? The report file was key-investigation export output — enforce a capture-time redaction wrapper for every diagnostic, and keep smoke artifacts (config copies, page snapshots) in auto-pruned temp dirs.
What would this supply-chain / third-party risk master suggest? Investigate which tool wrote report.20260919*.json at 21:11 on 09-19 (minutes before the21:14 PQC bundle rewrite that dropped provider keys) — the two events look related and the writer may still run.
What would this systems-architecture / devops / infrastructure master suggest? Make `localrouter start|ensure` and the ensure-timer take one lock so restarts can never double-spawn build/index.js, and teach the CLI to wait out the PQC pre-listen phase before reporting "no server".
What would this reliability / verification master suggest? Codify the scroll/search preservation scenarios as a DOM-level regression test (the suite already parses the page scripts) and add a git-history secret scan to CI so GH013 is caught before push time.
What would this governance / sovereignty master suggest? Record the incident in the PRD/DOX chain as a binding rule — diagnostic artifacts with key material are contraband, redacted at capture — with the rewrite SHA map as the audit trail.
What would this terminal-orchestration / swe-bench master suggest? When push protection blocks, prefer an isolated-clone filter-branch over stashing operator edits in the live checkout — the whole remediation stayed non-destructive to in-flight work that way.
What would this master-orchestration / self-prompting master suggest? The 09-20 loopback-only record (TASK.2026-09-20.router-loopback-only.md) rode along in today's docs commit so every task record now travels with push — keep closing record-layer loops at checkout time, not next session.
What would this developer-experience / ergonomics master suggest? Surface "list scroll kept" feedback per provider group (e.g. a sticky count) so users trust the new state preservation, and document the rotate-key step in PROVIDER_ENDPOINTS.md's operator checklist.
