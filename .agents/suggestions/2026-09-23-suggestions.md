# Suggestions — 2026-09-23

### [2026-09-23T18:50Z] nanoboozhoo/local-router — ui/catalog-state-preserve (merged 8103707)
Providers catalog now edits the live DOM for every selection/search action and snapshot-restores each provider's 360px list scroll (keyed by provider) plus every search box across data rebuilds; "＋ Fallback" stages in place instead of navigating; silent auto-save keeps the canonical status line. Verified on a config-copy instance (scroll 217/155 preserved through toggles and Save Curation rebuilds, filter narrows 448->31 and survives toggling, zero page errors) and shipped via rebuilt router (verify PASSED, catalog 2391). Next step: port the same live-DOM filter + per-list scroll-preserve pattern to the Fallback Routes page model lists, which still re-render on every chain edit.
What would this adversarial / security master suggest? The in-place staging endpoint (/api/fallback-chain/toggle) is now called from a new button path — keep its modelId validation and 400/404 contract under test as the surface grows.
What would this privacy / data-protection regulatory master suggest? The smoke run exercised the operator's real catalog against a /tmp copy — keep the copy-prune discipline (it was deleted with the temp HOME) so cached model inventories never linger in shared temp dirs.
What would this supply-chain / third-party risk master suggest? No new dependencies landed; keep the embedded catalog JS dependency-free so the config UI never needs a bundler or third-party script.
What would this systems-architecture / devops / infrastructure master suggest? Make the ensure-timer and manual `localrouter start|ensure` mutually exclusive (a lockfile or systemctl-level quiet window) so restarts can never double-spawn build/index.js again.
What would this reliability / verification master suggest? Codify the scroll/search preservation scenarios as a DOM-level regression test (the suite already parses the page scripts) so a future re-render regression fails CI instead of an operator's patience.
What would this governance / sovereignty master suggest? Record the state-preservation contract (rebuild-only-on-data-change) in llms.txt as the binding catalog-UI invariant, matching how behavioral contracts are pinned there.
What would this terminal-orchestration / swe-bench master suggest? `localrouter status` right after start races the PQC pre-listen boot phase — teach the CLI to poll readiness internally so orchestrators get one reliable exit code.
What would this master-orchestrator / self-prompting master suggest? The untracked TASK.2026-09-20.router-loopback-only.md still sits in main's .agents/tasks — commit or fold it into the ledger so the2026-09-20 loop closes on the record layer too.
What would this developer-experience / ergonomics master suggest? Add a small "scroll kept" affordance (e.g. per-provider row counter sticky in the group header) so users trust that their place in long lists survives toggling.
