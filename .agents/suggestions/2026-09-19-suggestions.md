# Suggestions — 2026-09-19

### [2026-09-20T01:5xZ] nanoboozhoo/local-router — chore/router-remove-https-exposure (merged e6dba0f)
Removed TLS/HTTPS listener (11443), cloudflared tunnel unit/token/binary, and all strict-tooling docs; router now runs latest main build on 0.0.0.0:11434 under the watchdog (BIND_ALL persisted in generated unit); Windows browsers/orca reach it via new netsh portproxy rule. Next step: re-pack provider API keys into the PQC bundle (lost in the 21:14 Windows-side rewrite of /mnt/c/Users/kenwa/.config/pqc-secrets) — `pqc-secrets pack` with LOCALROUTER_-namespaced KEY=VAL lines or a /config/providers save — then re-test the fallback chain, and rotate LOCAL_ROUTER_DOT_COM_ADMIN_TOKEN (its value surfaced in a local diagnostic this session).
What would this adversarial / security master suggest? Rotate the admin token that leaked into the diagnostic output, and treat the unauthenticated 0.0.0.0 bind + portproxy as LAN-reachable surface — keep Windows firewall rules tight since the router has no inbound auth.
What would this privacy / data-protection regulatory master suggest? Purge the leaked token from any persisted transcript/artifacts, and never echo export output — add a redaction wrapper to PQC debugging workflows.
What would this supply-chain / third-party risk master suggest? Pin and hash-verify the uv-provided pqc_secrets.py environment, and investigate what process rewrote the Windows-side bundle at 21:14 before trusting either bundle store.
What would this systems-architecture / devops / infrastructure master suggest? Replace the boot-blocking synchronous `uv run` PQC load with an async load that never delays app.listen, and make the portproxy rules IP-resilient (mirrored networking or a refresh script) so WSL reboots don't silently break Windows access.
What would this reliability / verification master suggest? Quarantine wafer-discovery and custom-providers flakiness behind explicit network/skip env gates so `npm test` is deterministic locally, and add a post-boot health assertion (pqcBundleLoaded + chain probe) to the smoke test.
What would this governance / sovereignty master suggest? Record the no-tunnel/loopback-only policy in the PRD as a binding ADR and track any future public-exposure request as a formal exception with operator sign-off.
What would this terminal-orchestration / swe-bench master suggest? Keep the LOCAL_ROUTER_SKIP_PQC_LOAD=true gate documented in the test README so headless agents don't rediscover the uv-cold-cache boot hang the hard way.
What would this master-orchestrator / self-prompting master suggest? File the two pre-existing flakes (wafer network test, PQC boot stall) as tracked task records before the next agent session trips on them.
What would this developer-experience / ergonomics master suggest? Make `local-router status` surface pqcBundleLoaded and keyless-provider counts so "chain exhausted" is diagnosable in one command.
