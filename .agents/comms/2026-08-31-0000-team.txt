# AGENTS COMMS — 2026-08-31

Protocol: append-only ledger for concurrent agents in this repo. Entries:
`checkin` → `update` → `intent-merge` → `checkout`. Bracket every input/output
with `start:` / `end:` ISO-8601. One block per input→output unit; abandoned
work still closes with `end:` + `status: abandoned`. Subagents set `parent:`.
Appending to this file is the only permitted edit outside a worktree.

---

- id: mac-checkout-1
  agent: mac-agent
  branch: chore/comms-2026-08-31
  start: 2026-08-31T01:30:00Z
  end: 2026-08-31T01:45:00Z
  scope: bin/pqc-secrets(.darwin-arm64), build/, runtime state
  objective: vendor PQC wrapper v1.2.1 live; router running on fresh build
  input: operator — start local-router for key/model configuration; coordinate with wtf hub
  output: >
    Vendored bin/pqc-secrets + darwin-arm64 refreshed to ainish-coder v1.2.1
    (merges 50444ff, 8d12b3b): prompt-free bootstrap + never-TTL session
    (persists until vault lock / shutdown / reboot; sleep-safe). tsc rebuild
    from current main. Stale Aug-26 daemon (pid 29826, pre-vault code) killed.
    Router LIVE persistent at 127.0.0.1:11434 — loopback-only by design,
    Windows runs its own instance. `[PQC] Loaded 1 provider key(s):
    modal-proxy`; remaining LOCALROUTER_* keys pending operator input
    (pack flow, picked up on next boot). Cross-machine coordination runs over
    the wtf hub (192.168.1.68:7800, bin 3 + comms channel mac-win-pipeline
    c2746f55ae9403a4fcf54579ca83230d); handoff sheet lives in
    wtf-is-going-on-mcp .agents/handoffs/HANDOFF.2026-08-29-machine1.md.
  status: merged
  blockers: none
