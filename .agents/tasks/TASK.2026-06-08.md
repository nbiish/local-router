# TASK.2026-06-08 — DOX index bootstrap

## Goal

Create a complete DOX hierarchy rooted at `llms.txt` with child `llms.txt` files in every durable folder.

## Plan

- Scan repo tree for folders with durable purpose.
- Draft concise child `llms.txt` files per DOX contract.
- Update root `llms.txt` Child DOX Index.
- Verify all `llms.txt` files present.

## Result

- Created 15 child DOX files plus updated root index.
- Worktree: `docs/dox-index` from `develop`.
