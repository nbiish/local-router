# TASK — thinking proxy toggle

Branch: feat/routing-thinking-toggle

- toggle off → passthrough
- toggle on → proxy levels
- AGENTS worktree cleanup
- remove stale worktrees

####

`enabled` on thinking-config; UI checkbox; sanitize skips overrides when false.
